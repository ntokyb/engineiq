# Public EngineIQ API (must match CORS AllowedOrigins in API appsettings)
NEXT_PUBLIC_ENGINEIQ_API_URL=http://localhost:5056
# After sign-up, deep-link to portal login
NEXT_PUBLIC_PORTAL_URL=http://localhost:3001
# Optional — DPA PDF link on sign-up (must match Trust:DpaPdfUrl in API when possible)
# NEXT_PUBLIC_DPA_PDF_URL=https://engineiq.co.za/legal/dpa.pdf
